version: 1
formatters:
  brief:
    format: '%(levelname)s - %(message)s'
  simple:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
loggers:
  thematic-fit-estimation.core:
    level: INFO
    handlers: [console]
  thematic-fit-estimation.main:
    level: INFO
    handlers: [console]
  thematic-fit-estimation.utils:
    level: INFO
    handlers: [console]
